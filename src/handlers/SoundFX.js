export class SoundFX {
    constructor(){

        // const AudioCtx = window.AudioContext || window.webkitAudioContext;
        this.audioContext = new AudioContext();
        this.loadAudio = createAudioLoader(audioContext);

        this.audioContext.d
    }
}