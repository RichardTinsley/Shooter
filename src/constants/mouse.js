export const OBJECT_TYPES = {
    NORMAL: "normal",
    ENEMY: "attack",
    TOWER: "select",
    MENUITEM: "select",
}

export const NULL_OBJECT = {
    type: OBJECT_TYPES.NORMAL,
};

export const SIZE = 3;