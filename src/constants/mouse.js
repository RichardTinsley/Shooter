export const CURSOR_TYPES = {
    NORMAL: "normal",
    ENEMY: "attack",
    TOWER: "select",
    MENUITEM: "select",
}

export const NULL_OBJECT = {
    type: CURSOR_TYPES.NORMAL,
};

export const SIZE = 3;