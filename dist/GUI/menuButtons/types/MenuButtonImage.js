import { MenuButton } from "../MenuButton.js";
export class MenuButtonImage extends MenuButton {
    constructor(setScreen, label) {
        super(setScreen, label);
        this.setScreen = setScreen;
        this.label = label;
    }
    draw(ctx) {
        super.draw(ctx);
    }
}
//# sourceMappingURL=MenuButtonImage.js.map